{
  "name": "catraz-vault-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Initial smart contract architecture for Catraz Vault",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network hardhat",
    "deploy:local": "hardhat run scripts/deploy-local.js --network hardhat",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.15"
  }
}
